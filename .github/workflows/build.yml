name: Build MAGOSTunnel.exe

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Test runner
        run: echo "Linux runner funciona"
